# feat(ai-data): 프로그램 사례 DB 스키마 설계 및 Prisma 마이그레이션

## 관련 이슈

- Closes #71
- 상위 이슈: #56
- 선행 이슈: #57, #67

## 작업 배경

#67에서 생성한 금정구 프로그램 크롤링 결과를 PostgreSQL에 저장할 수 있도록 Prisma 모델과 마이그레이션을 추가했습니다.

이번 PR은 데이터 저장 구조를 만드는 작업입니다. 크롤링 JSON의 실제 DB 적재, Upsert 로직, Express API 및 n8n 연동은 포함하지 않습니다.

## 주요 변경 사항

### 1. 크롤링 결과 분석 및 스키마 설계 문서 작성

- 최종 프로그램 DTO 349건 분석
- 프로그램 회차 20건 분석
- 첨부파일 237건 분석
- 프로그램 식별자와 URL 중복 없음 확인
- 프로그램 내부 회차 번호 중복 없음 확인
- JSON 필드와 Prisma 필드의 타입·nullable 대응 관계 문서화
- 후속 Upsert 구현 시 주의사항 문서화

### 2. Prisma 모델 추가

다음 모델을 추가했습니다.

- `ProgramCase`: 프로그램 기본 정보와 크롤링 원문 저장
- `ProgramCaseSession`: 프로그램의 회차별 날짜와 활동 정보 저장
- `ProgramCaseAttachment`: 첨부파일명, URL, 형식 및 추출 상태 저장

모델 관계는 다음과 같습니다.

```text
ProgramCase 1 ── N ProgramCaseSession
ProgramCase 1 ── N ProgramCaseAttachment
```

부모 프로그램 삭제 시 관련 회차와 첨부파일 메타데이터가 함께 삭제되도록 `onDelete: Cascade`를 적용했습니다.

### 3. 중복 방지 및 조회 인덱스 적용

- 프로그램 Upsert 자연키: `sourceType + sourcePostId`
- 회차 중복 방지: `programCaseId + sessionNumber`
- 첨부파일 중복 방지: `programCaseId + fileUrl`
- 접수 상태 조회 인덱스: `applicationStatus`
- 교육 기간 조회 인덱스: `educationStartDate`, `educationEndDate`

### 4. 날짜와 원문 보존

교육 시작일과 종료일은 검색·정렬을 위한 `DateTime` 값과 크롤링 원문 문자열을 함께 저장하도록 설계했습니다.

회차 날짜도 파싱된 `sessionDate`와 원문 `dateText`를 함께 저장하여 향후 다른 날짜 형식이나 파싱 실패에 대응할 수 있도록 했습니다.

### 5. PostgreSQL 마이그레이션 추가

`20260719233000_add_program_case_models` 마이그레이션을 추가했습니다.

- 신규 테이블 3개 생성
- 외래키와 `ON DELETE CASCADE` 적용
- 복합 unique 제약조건 생성
- 조회용 인덱스 생성
- 긴 원문은 PostgreSQL `TEXT`로 저장
- 파싱 경고는 `JSONB`로 저장

기존 테이블이나 컬럼을 삭제·수정하지 않습니다.

## 변경 파일

- `apps/backend/prisma/schema.prisma`
- `apps/backend/prisma/migrations/20260719233000_add_program_case_models/migration.sql`
- `docs/database/PROGRAM_CASE_SCHEMA.md`

## 검증 결과

다음 검증을 통과했습니다.

```bash
npx prisma format
npx prisma validate
npx prisma generate
npm run build
```

- Prisma 스키마 포맷 성공
- Prisma 스키마 검증 성공
- Prisma Client 생성 성공
- TypeScript 빌드 성공

## DB 적용 여부

연결된 `mo***` PostgreSQL DB의 `public` 스키마에서 `User`, `Interest`, `UserInterest`, `CommunityPost` 등 기존 서비스 테이블을 확인하여 현재 프로젝트 DB로 판단했습니다.

최초 확인 시 DB에만 존재하던 `20260719090000_create_board_post` 마이그레이션은 실제 `BoardPost` 메타데이터를 바탕으로 SQL을 재구성했습니다. 재구성한 파일의 SHA-256 체크섬이 DB의 `_prisma_migrations` 기록과 정확히 일치함을 확인한 뒤 로컬 migration history에 복원했습니다.

```text
복원한 마이그레이션:
20260719090000_create_board_post

적용한 마이그레이션:
20260719233000_add_program_case_models
```

이력 정상화 후 `prisma migrate deploy`를 실행하여 프로그램 사례 마이그레이션을 적용했습니다. `prisma migrate status`에서 5개 마이그레이션이 모두 적용된 최신 상태임을 확인했습니다.

PostgreSQL 메타데이터를 통해 다음 내용을 검증했습니다.

- 신규 테이블 `ProgramCase`, `ProgramCaseSession`, `ProgramCaseAttachment`
- 회차·첨부 외래키와 `ON DELETE CASCADE`
- 프로그램·회차·첨부의 복합 unique
- 접수 상태 및 교육 시작·종료일 index
- 긴 원문의 `TEXT`, 파싱 경고의 `JSONB`, 날짜의 timestamp 타입
- nullable 설정과 Prisma 스키마의 일치
- 신규 테이블이 모두 0건이며 실제 크롤링 JSON이 적재되지 않았음

Prisma Studio에서 `ProgramCase`, `ProgramCaseSession`, `ProgramCaseAttachment` 테이블이 표시되고 각 테이블이 0건인 상태를 직접 확인했습니다. 또한 `prisma migrate status`, `information_schema`, `pg_catalog`, `_prisma_migrations`, Prisma Client 생성으로 교차 검증했습니다.

`prisma migrate resolve`, `migrate reset`, `db push`는 실행하지 않았습니다.

## 제외 범위

- 크롤링 JSON 349건의 실제 DB 저장
- JSON import 스크립트
- PostgreSQL Upsert 구현
- Express 프로그램 API
- n8n과 Express API 연동
- 첨부파일 다운로드 및 본문 추출
- 추천·검색·RAG 기능

## 리뷰 참고 사항

- `applicationStatus`와 `extractionStatus`는 외부 원천 값의 확장 가능성을 고려해 Prisma enum이 아닌 `String`으로 유지했습니다.
- 349건 중 344건의 `sessions`가 빈 배열이므로 회차는 선택적인 1:N 관계로 분리했습니다.
- 크롤링 실행 통계 파일은 프로그램 도메인 데이터가 아니므로 `ProgramCase` 필드에 포함하지 않았습니다.
- 후속 Upsert에서는 원천에서 사라진 회차와 첨부파일까지 동기화할 수 있도록 트랜잭션 기반 비교 또는 자식 전체 교체 전략이 필요합니다.

## 체크리스트

- [x] 실제 크롤링 DTO 구조 분석
- [x] Prisma 모델 3개 추가
- [x] 복합 unique 및 조회 인덱스 적용
- [x] Cascade 관계 적용
- [x] 마이그레이션 SQL 검토
- [x] Prisma 스키마 검증
- [x] Prisma Client 생성
- [x] TypeScript 빌드
- [x] 연결된 DB가 현재 프로젝트 DB인지 읽기 전용으로 확인
- [x] 누락된 `20260719090000_create_board_post` 로컬 이력 복구
- [x] 복원 SQL과 DB migration 체크섬 일치 확인
- [x] 안전한 개발 DB에 마이그레이션 적용
- [x] 신규 테이블·제약조건·인덱스 메타데이터 검증
- [x] Prisma Studio에서 신규 테이블 확인
